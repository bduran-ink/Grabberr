Grabber — minimal demo

Start backend:
```
cd backend
npm install
npm start
```

Serve frontend:
```
npx --yes http-server "frontend" -p 3000
```

Open: `http://127.0.0.1:3000` and `http://127.0.0.1:4000/products`


